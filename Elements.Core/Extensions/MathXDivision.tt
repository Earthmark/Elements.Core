<#@ template debug="false" hostspecific="false" language="C#" #>
<#@ assembly name="System.Core" #>
<#@ import namespace="System.Linq" #>
<#@ import namespace="System.Text" #>
<#@ import namespace="System.Collections.Generic" #>
<#@ import namespace="System.Reflection" #>
<#@ output extension=".cs" #>
// Autogenerated!!! DO NOT MODIFY!
// Generated on: <#= DateTime.Now #>
// This was created because the rules for dividing numbers can get quite complicated. For example https://stackoverflow.com/a/26595091/2095344

using System;
using System.Linq;
using System.Runtime.CompilerServices;

namespace Elements.Core
{
    
    public static partial class MathX
    {
<#
        var types = new string[] { "uint", "ulong", "int", "long"};
        var problematicTypes = new string[] { "int", "long"};
#>

<#
        foreach(var t in types)
	    {
#>

        public static bool CanDivideBy(<#=t#> divisor)
        {
            return divisor != 0;
        }

        public static bool CanDivide(<#=t#> dividend, <#=t#> divisor)
        {
            <# if(Array.IndexOf(problematicTypes, t) != -1) { #>     
            if (dividend == <#=t#>.MinValue && divisor == -1)
                return false;
            <#}#>

            return divisor != 0;
        }

<#
        for (int i = 2; i<5; i++) {
#>
        // Dividing <#=t#><#=i#> by <#=t#><#=i#>
        public static bool CanDivide(in <#=t#><#=i#> dividend, in <#=t#><#=i#> divisor)
        {
            return <#if(i >= 2){#>MathX.CanDivide(dividend.x, divisor.x) && MathX.CanDivide(dividend.y, divisor.y)<#}#>
<#if(i >= 3){#> && MathX.CanDivide(dividend.z, divisor.z) <#}#>
<#if(i == 4){#> && MathX.CanDivide(dividend.w, divisor.w) <#}#>;
            
        }

        // Dividing <#=t#><#=i#> by <#=t#>
        public static bool CanDivide(in <#=t#><#=i#> dividend, <#=t#> divisor)
        {
            return <#if(i >= 2){#>MathX.CanDivide(dividend.x, divisor) && MathX.CanDivide(dividend.y, divisor)<#}#>
<#if(i >= 3){#> && MathX.CanDivide(dividend.z, divisor)<#}#>
<#if(i == 4){#> && MathX.CanDivide(dividend.w, divisor)<#}#>;
            
        }

        // Dividing <#=t#> by <#=t#><#=i#>
        public static bool CanDivide(<#=t#> dividend, in <#=t#><#=i#> divisor )
        {
            return <#if(i >= 2){#>MathX.CanDivide(dividend, divisor.x) && MathX.CanDivide(dividend, divisor.y)<#}#>
<#if(i >= 3){#> && MathX.CanDivide(dividend, divisor.z)<#}#>
<#if(i == 4){#> && MathX.CanDivide(dividend, divisor.w)<#}#>;
            
        }

        public static bool CanDivideBy(in <#=t#><#=i#> divisor)
        {
            return <#if(i >= 2){#>divisor.x != 0 && divisor.y != 0<#}#>
<#if(i >= 3){#> && divisor.z != 0<#}#>
<#if(i == 4){#> && divisor.w != 0<#}#>;
            
        }

<#
        }
#>


<#
        }
#>

      

    }
}
